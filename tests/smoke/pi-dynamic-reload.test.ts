// Root-path shim for the documented command:
// npm run test:smoke -- tests/smoke/pi-dynamic-reload.test.ts
// The executable test lives under tests/smoke/contracts so the existing
// package.json smoke glob (tests/smoke/**/*.test.ts) discovers it.
